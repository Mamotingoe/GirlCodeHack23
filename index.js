import React from 'react';
import ReactDOM from 'react-dom';
import AllyBotApp from './AllyBotApp';
import './index.css'; // Make sure to include your Tailwind CSS file

ReactDOM.render(
  <React.StrictMode>
    <AllyBotApp />
  </React.StrictMode>,
  document.getElementById('root')
);
